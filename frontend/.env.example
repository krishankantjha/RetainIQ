VITE_API_BASE_URL=
# Leave empty in local dev to use the Vite proxy (recommended). Or set http://127.0.0.1:8000
# Optional: credentials for the "View demo dashboard" button (defaults: admin / password)
# VITE_GUEST_USERNAME=admin
# VITE_GUEST_PASSWORD=password
